/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:['images.unsplash.com','aceternity.com ', 'www.boat-lifestyle.com', 'cdn.shopify.com']
    }
};

export default nextConfig;
